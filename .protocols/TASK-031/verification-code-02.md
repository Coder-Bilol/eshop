# TASK-031 Independent Functional Verification Code 02

## Findings

- HIGH: The documented no-source path fails with the real production composition.
  `apps/storefront/app/layout.tsx:19` mounts `CartProvider` with
  `restoreOnMount={false}`, so a fresh completion with no stored guest reference
  keeps the cart controller in `idle`. `mergeAfterAuthentication()` therefore
  returns `{ result: null, state: { status: "idle", cart: null, error: null } }`,
  but `apps/storefront/components/auth-completion.tsx:89-96` accepts no-source only
  when the synthetic state is `empty`. An independent composition probe reproduced
  the actual handoff and its classification as `merge_blocked`. A buyer who signs in
  without a guest cart cannot reach `authenticated_ready` or consume the return path.
- MEDIUM: The claimed malformed-handoff fail-closed coverage is incomplete.
  `resolveCartReadiness()` accepted `merged` for a `ready` state carrying an error,
  a `ready` state whose operation was still `restore`, and a merge result missing
  `source_cart_id` and `replayed`; it also accepted no-source while operation was
  still pending. These contradictory shapes are not generated by the current cart
  controller, but the runtime handoff type is not discriminated and the task
  explicitly requires malformed results to fail closed.

- Role: Reviewer
- Mode: scheduler
- Tier: T3
- Retry reviewed: 1/2
- Packet: `PACKET-TASK-031-R9`, status `ready`
- Packet hash: exact match,
  `sha256:a7ccca7393e701f948f09e86410f0b319d9b40472aa31f712f2ed42ee6ffcdf6`

VERDICT: FAIL

## Acceptance Results

- PASS: Equal Google and VK ID actions use the existing AuthProvider boundary.
  The exclusive action guard rejects a duplicate start while the first action is
  pending; pending, sanitized failure, and retry UI remain present.
- PASS: Completion accepts only one allowlisted provider and one coarse status,
  rejects extra/duplicate/unknown query values and fragments, and removes callback
  data from the visible URL before rendering completion state.
- PASS: A validated merge result with a restored `ready` cart matching the backend
  target reaches `authenticated_ready`.
- FAIL: The actual no-reference CartProvider handoff is `null` plus `idle`, not the
  synthetic `null` plus `empty` fixture. The required no-source success flow is
  therefore blocked in a fresh production completion route.
- PASS: `backend_error`, merged `empty`, the focused malformed fixture, unknown
  status, rejected merge, and auth-required merge do not reach readiness. Errors
  remain coarse and recoverable.
- FAIL: Additional contradictory/malformed handoff forms still reach readiness, so
  malformed input is not consistently fail-closed.
- PASS: Return-path consumption was independently probed across `processing`,
  `cart_merge_pending`, `merge_blocked`, `auth_cancelled`, and `auth_failed`; no
  consume call occurred. `authenticated_ready` also requires a current session.
- PASS: The production completion controller deterministically invalidates stale
  fulfillment on auth loss, retry replacement, and unmount. Same-mount duplicate
  merge starts are rejected, and the login action is one-flight.
- PASS with integration gap: controller-level concurrency coverage is deterministic
  and does not require unavailable browser tooling, but its no-source fixture does
  not exercise the actual CartProvider contract. Source assertions alone did not
  catch this production composition mismatch.
- PASS: Source/privacy inspection found no rendered callback code/state/token, raw
  backend/provider error, customer ID/email, dangerous HTML, or browser auth-token
  persistence. Synthetic tests contain no production PII or secrets.
- PASS: Runtime/test changes remain within packet scope. No backend provider, FT-003
  merge semantics, checkout form, order, inventory, payment, or Medusa Core change
  was attributed to TASK-031.

## Commands And Evidence

- PASS: raw SHA-256 comparison of packet R9 and the authoritative task record.
- PASS: `node scripts/mb-doctor.mjs --strict`; 0 errors, two unrelated upstream
  dependency warnings.
- PASS: `npm --workspace apps/storefront run test -- auth-ui`.
- PASS: `npm --workspace apps/storefront run test`; all nine storefront suites.
- PASS: `npm --workspace apps/storefront run typecheck`.
- PASS: `npm --workspace apps/storefront run build`; `/login` and
  `/auth/complete` generated.
- PASS: `node scripts/mb-lint.mjs`; 118 files.
- PASS: `git diff --check`; no whitespace errors, only line-ending warnings.
- PASS: independent controller probe for documented merge success,
  backend-error/empty/malformed/unknown blocking, blocked-state return-path
  non-consumption, auth-loss/retry invalidation, and unmount invalidation.
- FAIL: independent production-composition no-source probe. With the root layout's
  `restoreOnMount={false}` and empty cart-reference storage, the actual handoff was
  `result: null`, `state.status: idle`, and `resolveCartReadiness()` rejected it.
- FAIL: expanded malformed probe. `ready` plus error, `ready` plus pending restore,
  incomplete merge metadata, and no-source plus pending operation were accepted.

## T3 And Closure Recommendation

- REQUEST_CHANGES. TASK-031 is not closure-eligible and dependents must not be
  promoted. Retry 2/2 should align no-source handling with the actual CartProvider
  handoff and make contradictory/malformed readiness fail closed, then repeat both
  independent T3 gates.
- Human checkpoint evidence is explicit in the operator request and implementation
  handoff. Rollback/recovery remains credible: revert the scoped UI/test changes or
  disable provider starts; after suspected compromise rotate provider/session
  secrets, invalidate sessions, and preserve durable customer/cart records.
- Reviewer changed no implementation, task/packet lifecycle, dependency, changelog,
  or sync state and did not run `/execute` or `/mb-sync`.

HUMAN_CHECKPOINT: done
ROLLBACK_RECOVERY_NOTE: present
