# TASK-030 Functional Verification Report Code 04

VERDICT: FAIL

## Findings

- HIGH: callback validation accepts arbitrary same-backend paths, a callback route
  for the wrong provider, and callback query variants instead of enforcing the
  exact provider callback destination required by the linked contract.
- MEDIUM: explicit `:443` on either approved provider is normalized away and
  accepted despite the recovery criterion requiring ports to be rejected.
- MEDIUM: a return-like URL fragment bypasses the query-only navigation guard.

## Evidence

- Packet R8 is `ready`; its task SHA-256 matches the authoritative T3 task.
- Real locations generated by the installed Google provider and project VK provider
  are accepted, correcting code-03. External/lookalike origins, HTTP, credentials,
  non-default ports, unsafe external/nested callbacks, and query return parameters
  reject.
- Every earlier session/logout race, failed-logout retention, cart cleanup retry,
  one-shot storage, `401`, credentials-include, and no-token behavior remains green.
- Focused auth suites, all eight storefront suites, typecheck, production build,
  backend VK smoke, Memory Bank lint/strict doctor, and diff check pass.
- Independent hostile probes reproduced all findings. Full evidence:
  `.protocols/TASK-030/verification-code-04.md`.

## Recommendation

- `REQUEST_CHANGES`. Do not close `TASK-030` or promote `TASK-031`, `TASK-032`, or
  `TASK-039`. Route any further recovery through explicit scheduler/operator
  ownership, then repeat `/verify` and `/red-verify`. No code, lifecycle, sync, or
  dependent state was changed by Reviewer.

HUMAN_CHECKPOINT: done
ROLLBACK_RECOVERY_NOTE: present
